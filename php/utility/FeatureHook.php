<?php
declare(strict_types=1);

// Screenshot SDK utility: feature_hook

class ScreenshotFeatureHook
{
    public static function call(ScreenshotContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
